

import 'package:shared_value/shared_value.dart';

SharedValue<bool> is_log_in = SharedValue(
    value: false, //initial value
    key: "is_log_in"
);
SharedValue<bool> check = SharedValue(
    value: false, //initial value
    key: "is_log_in"
);